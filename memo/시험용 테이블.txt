시험용 데이터베이스

스키마명 BOARDTEST
aA123456

CREATE TABLE BOARD(
	ID NUMBER(10) CONSTRAINT PK_BOARD PRIMARY KEY,
	SUBJECT VARCHAR2(100) CONSTRAINT NN_BOARD_SUBJECT NOT NULL,
	CONTENT CLOB CONSTRAINT NN_BOARD_CONTENT NOT NULL,
	REGDT DATE DEFAULT SYSDATE,
	MODDT DATE
);

CREATE SEQUENCE SEQ_BOARD;

SELECT * FROM BOARD;

깃 레포지토리  URL 첨부할것

